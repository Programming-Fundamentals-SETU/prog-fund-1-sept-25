# 3. Exercises

Attempt the following exercises on loops.  For each exercise:

- Add a method to the Loops class (as was done in the demo step).

- use the global Scanner object to read in data from the user, if required.

- call your writen method from the Driver class, you may need to add another method here too to keep your code neat.

- Remember, the object of this lab is to give you practice with loops in an Array context.  So it is important you utilise loops throughout.

*You may need to refer back to your lecture notes during this lab*


## Exercise 1 - Construct the Array

Write a method to:

- construct the array to the size (int) that is passed to it
- your Driver class should ask the user to enter the size.

In Loops - add 2 fields, one for the Array of Strings and one to hold the total
~~~
    String names[];
    int total = 0;
~~~

In Loops - add a method that accepts an int as a parameter and constructs the Array
~~~
    public void sizeArray(int size){
        names = new String[size];
    }
~~~

## Exercise 2 - populate the Array

In Loops - add a method that accepts a String as a parameter and adds it to the Array
~~~
public boolean addName(String name){
        this.names[total] = name;
        total++;
        return true;
    }

~~~

In Driver - you can add the following 2 methods to ask the user how big the array will be and adds the names

~~~
public void createNames(){
        System.out.println("How many names to enter");
        int namesNum = input.nextInt();
        loops.sizeArray(namesNum);
        getNames(namesNum);
    }

public void getNames(int num){
        input.nextLine();
        for(int i = 0; i<num; i++){
          System.out.println("Enter a name");
          String name = input.nextLine();
          if(loops.addName(name))
              System.out.println("Sucessfully Added");
          else
              System.out.println("Error adding "+ name);
          
        }
    }
~~~

## Exercise 4 - Improve the addName method

In the previous exercise, what would happen if you added more names than the array could hold?  Can you improve the addName method so that it doesn't add the names if the array is full

## Exercise 6 - Add a list method

In Loops, add a method called list names that returns a String with all the names that were entered in uppercase.

## Exercise 6 - Add a findName method

In Loops, add a method that accepts a String as a parameter and finds that name in the array.  It should return the index number of the name if found, or -1 if not found

## Exercise 7 - Add a flag based loop

In Loops, add a method that accepts a doesn't take in any parameters, it creates an array with given numbers and return whether the array contains an odd number.

Write the following in Loops.

~~~java

public boolean containsOdd(){
      int numbers[] = {4,6,8,7,10,12};
      boolean oddNumberInArray = false;
      
      for(int i = 0; i< numbers.length; i++)
      if (numbers[i] %2 == 1) {   //check if it is odd
         oddNumberInArray = true;
      }
      
      return oddNumberInArray;
        
  }

~~~

Can a method in Driver that calls this method and prints to the console if one of the numbers is odd

## Exercise 8 - Overload containsOdd()

Write another method called containsOdd, that takes a paramter of an int array, and returns a boolean indicating if it finds a odd number.  In Driver, write a method that askes the user to enter numbers, stores them in an array and passes that array to the new containsOdd method

## Exercise 9 - Sentinel Loop

Add the following method to Loops
~~~java

  
  public String printStars(int numStars){
      
        String s = "";
        for(int i = 0;i<numStars; i++){
            s+="*";
        }
        return s+"\n";
  }
~~~


In Driver, write a method that asks the user to keep entering a number to print that number of stars, enter 0 to stop.

~~~java

    public void printStars(){
        System.out.println("Enter number of stars (0 to stop)"); 
        int numStars = input.nextInt();
        while(numStars != 0){
            System.out.println(loops.printStars(numStars));
            System.out.println("Enter number of stars (0 to stop)"); 
            numStars = input.nextInt();
        }
    }
~~~

