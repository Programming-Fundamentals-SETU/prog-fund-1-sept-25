# Challenges

Ensure you have attempted the FortuneTeller from lab 1

## Challenge 1 - Overloaded Method

(futurePrediction(int age) should exit already, but create it if it doesn't)
- Predict the future based on the user’s star sign, and return a fun message.

| `futurePrediction(String starSign)`  -  Returns a message predicting something about the person based on thier starSign. 

We haven't done String methods yet, but to compare Stings you don't use == instead you use a method called .equals() or .equalsIgnoreCase()

e.g.
~~~
starSign.equalsIgnoreCase("Aries")
~~~